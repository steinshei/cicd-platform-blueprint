apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ .Values.nameOverride }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.rollout.replicas }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: {{ .Values.nameOverride }}
  template:
    metadata:
      labels:
        app: {{ .Values.nameOverride }}
    spec:
      containers:
        - name: {{ .Values.nameOverride }}
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.service.targetPort }}
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.service.targetPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
{{ toYaml .Values.resources | indent 12 }}
  strategy:
    canary:
      canaryService: {{ .Values.nameOverride }}-canary
      stableService: {{ .Values.nameOverride }}
{{- $steps := .Values.rollout.stepsWithoutAnalysis }}
{{- if .Values.rollout.enableAnalysis }}
{{- $steps = .Values.rollout.stepsWithAnalysis }}
{{- end }}
      steps:
{{ toYaml $steps | indent 8 }}
